import file
import tty

entry
	mut f1 = "sgnigniggigigigi"
	mut fi = file:stat(f1)

	tty:out(f1)
	tty:outln(fi? => "exists" || " does not exist")

	file:fileFree(fi)

	fi = file:stat("/")

	tty:out(fi.path())
	tty:outln(fi.isDir() => " is a dir" || " is not a dir")

	file:fileFree(fi)
/entry
